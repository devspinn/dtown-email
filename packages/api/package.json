{
  "name": "@yieldplat/api",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "dotenvx run -f ../../.env -- tsx watch src/server.ts",
    "build": "tsup",
    "deploy": "pnpm run build && wrangler deploy"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@hono/trpc-server": "^0.4.0",
    "@trpc/server": "^11.7.1",
    "@yieldplat/auth": "workspace:*",
    "@yieldplat/db": "workspace:*",
    "hono": "^4.10.5",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "tsx": "^4.19.2",
    "wrangler": "^4.47.0"
  }
}
