{
  "name": "yieldplat",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "db:push": "dotenvx run -- pnpm --filter @yieldplat/db run push",
    "db:studio": "dotenvx run -- pnpm --filter @yieldplat/db run studio",
    "db:generate": "dotenvx run -- pnpm --filter @yieldplat/db run generate"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "^1.51.1",
    "prettier": "^3.6.2",
    "turbo": "^2.6.1",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
